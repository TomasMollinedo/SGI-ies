import { Module } from '@nestjs/common';

/**
 * HU-14 — Tipos de comprobante. El controller y el service se agregan al
 * implementar la historia; por ahora el módulo existe solo para reservar la
 * estructura.
 */
@Module({
  controllers: [],
  providers: [],
})
export class TipoComprobanteModule {}
